/* 
 * JavaScript é tipagem fraca e tipagem dinamica
 * - Python é tipagem forte e dinâmica (não permite concatenar)
 * - Java é tipagem forte e estática (não permite reatribuir valores de outro tipo)
 */

const numero = 30; //number
const texto = "Exemplo"; // string

console.log(typeof numero, numero, typeof texto, texto, numero+texto)